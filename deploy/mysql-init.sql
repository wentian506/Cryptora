-- CRYPTORA MySQL bootstrap. Tables themselves are created by the backend
-- (SQLAlchemy create_all) on first start; Alembic migrations are roadmap.
CREATE DATABASE IF NOT EXISTS cryptora CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
